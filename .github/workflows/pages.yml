name: GitHub Pages

# Site is published from branch main / (root).
# Do not run official pages actions unless Source is "GitHub Actions".
on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Using branch deploy from repo root"
